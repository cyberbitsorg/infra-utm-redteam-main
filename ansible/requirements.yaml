---
# Pinned to a major so a breaking bump can't land silently. Widen deliberately.
collections:
  # community.general.timezone
  - name: community.general
    version: ">=13.0.0,<14.0.0"
  # ansible.posix.mount
  - name: ansible.posix
    version: ">=1.5.0,<3.0.0"
