---
# Pinned to a major so a rebuild is reproducible and a breaking major bump
# (renamed args, changed idempotence) can't land silently. Widen deliberately.
collections:
  - name: community.general
    version: ">=13.0.0,<14.0.0"
  - name: community.docker
    version: ">=5.0.0,<6.0.0"
  - name: ansible.posix
    version: ">=1.5.0,<3.0.0"
